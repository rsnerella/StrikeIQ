# StrikeIQ Backend Environment Variables
# Copy this file to .env and fill in your actual values

# Upstox API Credentials
UPSTOX_API_KEY=your_upstox_api_key_here
UPSTOX_API_SECRET=your_upstox_api_secret_here

# OAuth Configuration
UPSTOX_REDIRECT_URI=http://localhost:8000/api/v1/auth/upstox/callback

# Security
SECRET_KEY=your-secret-key-change-in-production-min-32-chars

# Frontend URL
FRONTEND_URL=http://localhost:3000

# Database Configuration
DATABASE_URL=postgresql://username:password@localhost:5432/strikeiq

# Redis Configuration
REDIS_URL=redis://localhost:6379
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_DB=0

# Upstash Redis Configuration (Optional - will use local Redis if not configured)
UPSTASH_REDIS_URL=redis://your-upstash-redis-url:port
UPSTASH_REDIS_TOKEN=your-upstash-redis-token
UPSTASH_REDIS_REST_URL=https://your-upstash-rest-url
REDIS_PROVIDER=auto  # Options: 'local', 'upstash', 'auto'

# Logging
LOG_LEVEL=INFO
